# 统一样式系统指南

## 已完成的工作 ✅

所有主要页面已统一使用 `layout-v2.jade`，导航栏和页脚在所有页面都显示，风格保持一致。

### 更新的页面

1. ✅ **首页** - `index-v2.jade`
2. ✅ **电影库** - `movielibrary.jade`
3. ✅ **电影详情** - `moviedetail.jade`
4. ✅ **推荐结果** - `recommendresult.jade`
5. ✅ **我的收藏** - `favorites.jade`

### CSS架构

```
layout-v2.jade (基础布局)
  ↓
  ├─ common.css (导航栏、页脚、按钮等共享样式)
  ├─ page-common.css (所有内容页面通用样式)
  └─ 页面专属.css (如 index-page.css，可选)
```

## 统一的设计系统

### 配色方案

所有页面使用统一配色：

- **主色调**: 紫色渐变 `linear-gradient(135deg, #667eea, #764ba2)`
- **辅助色**:
  - 黄色星星: `#ffc107`
  - 白色背景: `#ffffff`
  - 浅灰背景: `#f5f7fa`
- **文字色**:
  - 标题: `#2c3e50`
  - 正文: `#555`
  - 次要文字: `#7f8c8d`
  - 占位文字: `#95a5a6`

### 组件样式

#### 1. 页面头部 (`.page-header`)
```jade
.page-header
  h1
    i.fas.fa-icon
    | 页面标题
  p 页面描述
  .page-actions
    a.btn(href='...') 按钮
```

- 紫色渐变背景
- 圆角 15px
- 居中对齐
- 白色文字

#### 2. 控制栏 (`.controls-bar`)
```jade
.controls-bar
  .controls-group
    span.label 标签：
    a.control-btn(class='active') 选项
```

- 白色背景
- 圆角 12px
- 按钮圆角 20px
- 激活状态紫色渐变

#### 3. 电影卡片 (`.movie-card`)
```jade
.movie-card
  .card-top
    img(src='...')
    .card-overlay
      .overlay-content
        .overlay-rating 评分
        .overlay-action 查看详情
  .card-content
    h3 电影名
    .meta
      span.genre 类型
      span.rating 评分
  .card-actions
    button.action-btn.favorite-btn 收藏
```

- 统一高度 330px
- 圆角 12px
- 悬停上浮 8px
- 阴影效果
- 收藏按钮右上角

#### 4. 分页 (`.pagination`)
```jade
.pagination
  a.page-btn 上一页
  a.page-btn(class='active') 1
  a.page-btn 下一页
```

- 圆角 8px
- 激活状态紫色渐变
- 居中显示

#### 5. 空状态 (`.empty-state`)
```jade
.empty-state
  i.fas.fa-icon
  h3 标题
  p 描述
  a.btn.btn-primary(href='...') 按钮
```

- 大图标 80px
- 灰色主题
- 居中对齐

### 按钮样式

所有按钮使用统一的 `.btn` 类：

- **`.btn-primary`**: 紫色渐变，用于主要操作
- **`.btn-secondary`**: 灰色，用于次要操作
- **`.btn-login`**: 边框样式，用于登录
- **`.btn-signup`**: 紫色渐变，用于注册

所有按钮悬停时都有：
- 轻微上移 `-2px`
- 阴影增强

### 响应式断点

统一的响应式断点：

- **1200px**: 调整网格列数
- **992px**: 合并控制栏、调整详情页布局
- **768px**: 移动端布局，隐藏导航菜单
- **480px**: 小屏幕优化

## 页面特定说明

### 首页 (index-v2.jade)
- 轮播图自动播放
- 使用指南三步骤
- 评分入口带动画
- 个性化推荐区（登录后）

### 电影库 (movielibrary.jade)
- 排序按钮（默认、评分、热门、最新）
- 悬停显示遮罩和详情按钮
- 分页控制
- 收藏功能

### 电影详情 (moviedetail.jade)
- 背景图区域（渐变遮罩）
- 左侧海报 + 收藏按钮 + 评分卡片
- 右侧剧情简介 + 基本信息
- 信息网格布局

### 推荐结果 (recommendresult.jade)
- 头部操作按钮（返回首页、我的收藏）
- 推荐度显示
- 空状态引导去评分

### 我的收藏 (favorites.jade)
- 头部统计收藏数量
- 取消收藏动画
- 空状态引导

## 使用指南

### 创建新页面

1. **继承 layout-v2**:
```jade
extends layout-v2
```

2. **使用标准容器**:
```jade
block content
  .page-container
    .page-header
      h1 页面标题
    // 内容...
```

3. **使用统一组件**:
   - 头部：`.page-header`
   - 控制栏：`.controls-bar`
   - 卡片网格：`.movies-grid` + `.movie-card`
   - 分页：`.pagination`
   - 空状态：`.empty-state`

4. **添加JS（可选）**:
```jade
block extraJS
  script(src='/js/xxx.js')
```

5. **添加专属CSS（可选）**:
```jade
block extraCSS
  link(rel='stylesheet', href='/css/xxx-page.css')
```

## 测试步骤

1. **重启服务器**:
```bash
node movierecommend.js
```

2. **测试每个页面**:
   - ✅ 首页 `/` - 轮播、推荐
   - ✅ 电影库 `/movielibrary` - 排序、分页、收藏
   - ✅ 电影详情 `/movie/:id` - 背景图、信息、收藏
   - ✅ 推荐结果 `/recommendmovieforuser` - 推荐列表、收藏
   - ✅ 我的收藏 `/favorites` - 收藏列表、取消收藏

3. **检查统一性**:
   - ✅ 导航栏在所有页面显示
   - ✅ 页脚在所有页面显示
   - ✅ 颜色方案一致（紫色渐变）
   - ✅ 按钮样式一致
   - ✅ 卡片样式一致
   - ✅ 悬停效果一致

4. **测试响应式**:
   - 缩小浏览器窗口
   - 检查移动端布局
   - 确认导航菜单自动隐藏

## 优势

这个统一样式系统的优点：

1. ✅ **视觉一致**: 所有页面看起来是一个整体
2. ✅ **易于维护**: 修改 common.css 或 page-common.css 即可全局更新
3. ✅ **开发效率**: 新页面直接使用现成组件
4. ✅ **用户体验**: 统一的交互和反馈
5. ✅ **响应式**: 所有页面自动适配移动端

## 文件清单

### 布局文件
- `views/layout-v2.jade` - 基础布局模板

### 页面文件
- `views/index-v2.jade` - 首页
- `views/movielibrary.jade` - 电影库
- `views/moviedetail.jade` - 电影详情
- `views/recommendresult.jade` - 推荐结果
- `views/favorites.jade` - 我的收藏

### CSS文件
- `public/css/common.css` - 导航、页脚、按钮等共享样式
- `public/css/page-common.css` - 页面通用样式（头部、卡片、分页等）
- `public/css/index-page.css` - 首页专属样式（轮播、步骤等）

### JS文件
- `public/js/index-page.js` - 首页轮播功能
- `public/js/favorites.js` - 收藏功能

## 下一步

如果需要进一步定制，可以：

1. 调整 `common.css` 中的全局颜色变量
2. 调整 `page-common.css` 中的组件样式
3. 为特定页面添加专属CSS文件
4. 添加更多动画效果

现在所有页面风格统一，导航栏和页脚在所有页面都显示！🎉
