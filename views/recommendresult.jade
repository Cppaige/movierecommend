//- views/recommendresult.jade - 简化版本
html
  head
    title 个性化推荐结果
    style.
      body {
        background: #ffffff;
        font-family: 'Segoe UI', 'Microsoft YaHei', sans-serif;
        margin: 0;
        padding: 20px;
      }
      .header {
        text-align: center;
        margin-bottom: 40px;
        padding: 20px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border-radius: 10px;
      }
      .recommendation-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
        gap: 20px;
        max-width: 1200px;
        margin: 0 auto;
      }
      .movie-card {
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        padding: 15px;
        text-align: center;
        transition: transform 0.3s, box-shadow 0.3s;
      }
      .movie-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 5px 20px rgba(0,0,0,0.1);
      }
      .movie-poster {
        width: 100%;
        height: 250px;
        object-fit: cover;
        border-radius: 5px;
        margin-bottom: 10px;
      }
      .movie-title {
        font-weight: bold;
        margin: 10px 0;
        height: 40px;
        overflow: hidden;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
      }
      .movie-rating {
        color: #ffc107;
        font-size: 14px;
        margin-top: 5px;
      }
      .back-button {
        display: block;
        width: 200px;
        margin: 40px auto;
        padding: 12px 24px;
        background: #333;
        color: white;
        text-decoration: none;
        text-align: center;
        border-radius: 6px;
        font-weight: 500;
        transition: background 0.3s;
      }
      .back-button:hover {
        background: #555;
      }
  body
    .header
      h1 🎬 为您量身定制的电影推荐
      p 基于您的观影偏好，发现您可能会喜欢的电影

    .recommendation-grid
      if movieinfo && movieinfo.length > 0
        each movie in movieinfo
          .movie-card
            if movie.picture
              img.movie-poster(src=movie.picture, alt=movie.moviename)
            else
              .no-poster 暂无图片
            .movie-title= movie.moviename
            if movie.rating
              .movie-rating 推荐度: #{movie.rating.toFixed(1)}/5.0
      else
        .no-results
          h3 暂无推荐结果
          p 请先评价更多电影，让我们更了解您的喜好

    a.back-button(href='/movie-selection') ← 重新选择并评分